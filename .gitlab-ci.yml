image: amazoncorretto:20.0.2-al2023-generic

stages:
  - build
  - test

build:
  stage: build
  script:
    - chmod +x ./gradlew
    - ./gradlew assemble
  artifacts:
    paths:
      - build/libs/*.jar

test:
  stage: test
  script:
    - chmod +x ./gradlew
    - ./gradlew check clean
