with Ada.Text_IO; use Ada.Text_IO;

procedure IDENT0 is

    -- IDENT : Integer;

    begin    
    
        -- IDENT := 1;

        for i in reverse 1..1
            loop
                i := 1;
            end loop;
        end;

