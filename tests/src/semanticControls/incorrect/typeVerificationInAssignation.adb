with Ada.Text_IO; use Ada.Text_IO;

procedure test is

    a : integer := 0;
    b : integer := 0;
    c : character := 'a';
    d : character := 'b';
    e : boolean := true;
    f : boolean := false;

    a1 : integer;
    a2 : character;
    a3 : boolean;

    begin
        a1 := c;
        a2 := c;
        a3 := e;
    end test;
